<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-lg-6 col-md-8 col-sm-10">
      <div class="border rounded p-3 shadow-sm">
        <h1 class="text-center mb-3">Apagar Sua Conta</h1>
        <div class="text-center mb-3" id="sadface">
          <img src="/img/kaka.png" alt="Pessoa triste se despedindo">
        </div>
        <h1 class="text-center mb-3">Senhor {{usdados.nome}}</h1>
        <p class="text-center mb-3">Você está prestes a perder todas as vantagens que temos a oferecer</p>
        <form action="/pages/apagarcount/{{this.id}}" method="POST" class="text-center mb-3">
          <input type="hidden" name="id" value="{{this.id}}">
          <button type="submit" class="btn btn-danger btn-block">Apagar conta</button>
        </form>
      </div>
    </div>
  </div>
</div>
